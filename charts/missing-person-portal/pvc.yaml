apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: {{ include "missing-person-portal.fullname" . }}-pvc
spec:
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi