apiVersion: v1
kind: Service
metadata:
  name: {{ include "missing-person-portal.fullname" . }}
  labels:
    {{- include "missing-person-portal.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "missing-person-portal.selectorLabels" . | nindent 4 }}
