apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "missing-person-portal.fullname" . }}-test-connection"
  labels:
    {{- include "missing-person-portal.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "missing-person-portal.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
